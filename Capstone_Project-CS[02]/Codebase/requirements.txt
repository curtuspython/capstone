# CV Sorting using LLMs (CS[02]) - Python Dependencies
# Install with:
#   pip install -r requirements.txt
#
# Versions tested during development (Python 3.11+):

# Google Gemini LLM API (new SDK)
google-genai>=1.68.0

# LangChain - Orchestrator for prompt-based matching, score aggregation,
#             and structured output parsing
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-core>=0.3.0

# LlamaIndex - Semantic candidate-job matching framework
llama-index-core>=0.12.0
llama-index-llms-gemini>=0.4.0
llama-index-embeddings-gemini>=0.3.0

# Resume parsing
pypdf>=6.9.0              # PDF text extraction
python-docx>=1.1.0        # Word (.docx) text extraction
pyresparser>=1.0.6        # Structured information extraction from resumes

# NLP dependencies (used by pyresparser for entity recognition)
spacy>=3.0.0
nltk>=3.8.0

# PDF report generation
fpdf2>=2.8.0
